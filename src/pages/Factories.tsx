// claude:
/*
import { useLoaderData } from 'react-router-dom';

export default function Factories() {
  const { factories } = useLoaderData() as { factories: Factory[] };
  
  return (
    <div>
      <h1>My Factories</h1>
      {factories.map(factory => (
        <div key={factory.id}>{factory.name}</div>
      ))}
    </div>
  );
}
*/
